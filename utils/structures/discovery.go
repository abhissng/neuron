package structures

/*
// ServiceDiscoveryConfig represents the JSON configuration structure
type ServiceDiscoveryConfig struct {
	Services map[string]ServiceDefinition `json:"services"`
}

// ServiceDefinition defines a service's communication endpoints
type ServiceDefinition struct {
	Subject         string `json:"subject"`          // Primary processing subject
	RollbackSubject string `json:"rollback_subject"` // Compensation subject
	NextSubject     string `json:"next_subject"`     // Default next step
	IsTerminal      bool   `json:"is_terminal"`      // Whether this ends the workflow
}

// ExecutionHistory tracks completed steps for a transaction
type ExecutionHistory struct {
	CorrelationID types.CorrelationID
	Services      []string // Ordered list of executed services
}
*/
